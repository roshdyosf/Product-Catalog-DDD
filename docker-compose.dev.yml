version: "3.8"
services:
  app:
    build:
      context: .
      args:
        NODE_ENV: development
    volumes:
      - ./src:/app/src:ro
    environment:
      - NODE_ENV=development
    command: npm start-dev
